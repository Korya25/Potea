import 'package:flutter/material.dart';

class AppColors {
  // DARK THEME COLORS
  static const Color dBackPrimary = Color(0xFF181a20);
  static const Color dBackSecondary = Color(0xFF1f222a);

  static const Color primary = Color(0xFF0acf74);

  // Common colors
  static const Color textAndIconPrimary = primary;
  static const Color textAndIconWhite = Color(0xFFffffff);
  static const Color textAndIconBlack = Color(0xFF000000);
  static const Color textAndIconGrey = Color(0xff9e9e9e);

  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color red = Colors.red;

  // ========== Shimmer Colors ==========
  static const Color shimmerBase = Color(
    0xFF1A1A1A,
  ); // equivalent to Colors.grey[850]
  static const Color shimmerHighlight = Color(
    0xFF424242,
  ); // equivalent to Colors.grey[800]
}
