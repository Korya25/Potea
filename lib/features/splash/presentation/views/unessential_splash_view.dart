import 'package:flutter/material.dart';

class UnessentialSplashView extends StatelessWidget {
  const UnessentialSplashView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Unessential Splash')));
  }
}
