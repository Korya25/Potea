import 'package:flutter/material.dart';

class AppColors {
  // DARK THEME COLORS
  static const Color darkBackgroundPrimary = Color(0xFF25262a);
  static const Color darkBackgroundSecondary = Color(0xFF2d2e32);
  static const Color textAndIconWhite = Color(0xFFffffff);
  static const Color textAndIconBlack = Color(0xFF000000);
  static const Color textAndIconPrimary = primary;

  // Common colors
  static const Color primary = Color(0xFF1dc877);
  static const Color textAndIconGrey = Colors.grey;
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color red = Colors.red;
}
